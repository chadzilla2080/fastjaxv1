<div class="topbar">
  <div class="container">
    <div class="vertical-center">
      <%= link_to 'Home', root_path %> > <%= link_to @category.name, @category %> > <%= @subcategory.name %>
      <br />
      <br />
    </div>
  </div>
</div>
<div class="container">
  <ul class="list-margin">
    <% if @listings.present? %>
      <% @listings.each do |listing| %>

      <% end %>
    <% else %>
      <p>No listings found!</>
    <% end %>
  </ul>
</div>